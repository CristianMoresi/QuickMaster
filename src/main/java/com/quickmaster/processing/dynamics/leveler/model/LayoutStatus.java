package com.quickmaster.processing.dynamics.leveler.model;

/** Stable outcome of structural boundary selection. */
public enum LayoutStatus
{
    READY,
    INSUFFICIENT_FEATURES,
    TOO_MANY_SEGMENTS
}
