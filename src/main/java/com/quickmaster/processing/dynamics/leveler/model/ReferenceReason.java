package com.quickmaster.processing.dynamics.leveler.model;

/** Stable explanation for a raw shadow reference target. */
public enum ReferenceReason
{
    GROUP_REFERENCE,
    PAIR_REFERENCE,
    NOT_COMPARABLE,
    PROTECTED,
    INSUFFICIENT_LOUDNESS,
    AMBIGUOUS,
    STANDARD_VALIDATION_FAILED
}
